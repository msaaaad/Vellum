{
  "name": "@vellum/core",
  "version": "0.0.0",
  "private": true,
  "description": "Framework-agnostic canonicalization, hashing, and verification for Vellum audit chains.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
